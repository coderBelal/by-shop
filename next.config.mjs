/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.postimg.cc'], // Add the domain where your images are hosted
    },
  }
  
  export default nextConfig;
  